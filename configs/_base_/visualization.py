visualization = dict(
    enabled=True,
    save_dir='visualizations',
    save_stage='val',
    alpha=0.45,

    vis_prob=0.05,
    max_samples_per_epoch=0,
    vis_seed=42,

    image_folder_pattern='image_{image_id:06d}',
    ignore_index=255,
)